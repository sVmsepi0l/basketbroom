# hogwarts integration: sprint 3

the native creator kit dungeon now has **45% more enclosed volume**, with **356 saved-map collision queries passed**. its existing user-started play session passed **17/17 current-runtime checks** on september 15 at 12:07:47 utc, including an advancing world clock. a separate owned-session stop, clean-editor check and **27/27 dungeon/anchor checks** then passed. earlier normal-startup and pre-expansion play evidence remains below. the ue 5.8 standalone game remains a separate executable; native entry/return travel, match gameplay, publication and hogwarts multiplayer are still unproved.

## original roof amendment scope and preservation

[stage_hlck_pyramid_net.py](../Tools/stage_hlck_pyramid_net.py) uses the installed licensee ue 4.27 public editor apis and the existing owned source importer. it accepts only the exact installed phoenix project, the repo-mounted basketbroom plugin, the active basketbroom mod, a completed asset registry scan, no pie worlds and no dirty packages. one of the two existing mod maps must already be open.

the complete write allowlist is seven native packages: `BB_Arena_Port.umap`, `Basketbroom_DungeonMap.umap`, four `sm_bb_pyramid*` mesh assets and `M_BBPort_RoofNet.uasset`. original obj sources provide the geometry. the helper does not call the whole arena builder, copy ue 5 packages, create a match blueprint, change a datatable or `ModEdits.sql`, register an entrance, or invoke travel.

the roof rises from the existing 138-foot eaves to a 207-foot apex. its collision source has exactly five vertices and four triangular faces, with no horizontal base. native mesh collision is double-sided and complex-as-simple; decorative cord/rib meshes have no collision. the existing native rebound physical material is reused.

for both maps, staging checks the original ownership marker and preserves every unrelated actor's identity, transform, tags, folder, mesh, material and collision setup. it also preserves the native default game mode, playerstart tag and dungeon exit fields/components. the lower rebound net retains its existing label and uobject name; the cosmetic rename was removed to preserve actor identity. every overwritten package receives a verified byte backup outside shipping content before authoring.

before saving, and again after reloading each map, the helper queries the four roof faces, four hips and apex from both sides. the kit does not expose the ue 5 `HitResult.to_dict()` interface; these checks use the proven public hit/None profile trace contract, bracketing collision within one centimeter of each expected surface. five additional queries prove the old flat eave opening remains clear. this is 41 actual collision queries per map per audit, not a physics/gameplay simulation.

a successful stage requires the final content diff to remain inside the seven-package allowlist, installed map metadata and installed database hashes to remain unchanged, the registered entrance and saved sql to match their prior values, the original editor map to be restored, and both dirty-package lists to be empty. attempts have independent reports under `.local/hlck/pyramid-net-stage/<timestamp>/result.json`. `.local/hlck/pyramid-net-success.json` points only to a fully successful immutable result. a failed stage does not replace earlier successful evidence or discard unsaved editor work; its report identifies local backups for inspected recovery.

the read-only arena, dungeon and anchor inspectors recognize the amendment through an exact chain: original map hash, verified pre-stage byte backup, saved amended hash. they do not rewrite the september 12 arena/anchor receipts. the staged roof venue contains 905 generated arena actors, 18 original meshes and 19 port materials; the existing nine geometry checks and 27 complete dungeon/anchor checks remain mandatory, with the geometry roof check also running the actual collision audit.

## original roof amendment execution sequence

1. outside the editor, run `Mod/Tools/prepare_sources.py`, then `Tools/stage_hlck_pyramid_net.py --validate-sources`. refresh the manifest again if rules or source art changed meanwhile.
2. finish and close the standalone ue 5.8 editor before loading the memory-intensive kit. launch the creator kit from the signed-in epic games library, preserve the verified launcher authentication handoff and select the existing basketbroom mod. do not copy authentication arguments into files or start the unauthenticated ue4 executable.
3. write a fresh harmless request before enabling the existing editor bridge. open the saved owned dungeon through `load_hlck_dungeon.py` after its clean-editor checks. run `stage_hlck_pyramid_net.py` with `{"dry_run": true}` and inspect its new result. continue with `{"dry_run": false}` only when that exact preflight is ready.
4. read the new successful receipt, then run `inspect_hlck_dungeon_anchors.py` on the restored dungeon. all 27 checks must pass with current map hashes and actual roof collision. run `plan_hlck_dungeon_registration.py` to reverify the existing native registration without writing it again.
5. run `Tools/Inspect-HLCKStartup.ps1 -save` to inspect current shader-worker activity, memory and disk headroom before ordinary Play. let an existing compilation backlog settle before another play attempt. preserve actual database bytes using the corrected observer's built-in preflight. arm `test_hlck_dungeon_pie.py` through the bridge and use the normal editor play button; this kit exposes no verified reflected true-play starter, and simulate is not an equivalent test.
6. observe the corrected fixture and memory while it runs. it must identify the exact owned pie world, native phoenix game mode, game instance, player controller and possessed native character; then verify grounded spawn, runtime dungeon/subdivision extensions, existing entrance row, and exit interaction components/defaults. it gives startup at most ten minutes and an observed pie world at most three minutes, ending only the owned session through public `editor_end_play()`. the root operator should end the owned session sooner if memory pressure or a shader backlog makes the bounded check unhealthy.
7. verify no pie world, callback or dirty package remains. the observer may identify a native dynamic-database cache refresh; inspect its retained pre-play byte copies and report rather than restoring installed databases. actual first entry and return still require a separate test through native interactions. no generic map-open command is treated as travel proof.

## native authoring evidence and validation

the successful native receipt is `.local/hlck/pyramid-net-stage/20260915-012149-683145-fe0c3952/result.json` (september 14 local time / september 15 UTC). both maps passed exact nonroof snapshot comparisons before save and after reload, preserving 900 unrelated actors in the original arena and 901 in the dungeon. the stage performed 41 roof queries before and after saving each map, for **164 actual native trace calls**. the saved original dungeon was restored cleanly; registration readback, sql, installed database hashes and installed-map metadata remained unchanged. the final retry changed only the two maps and the owned collision mesh; already saved roof imports and material from the earlier attempt were reused inside the same seven-package scope.

the native collision rebuild is now proven. setting bodysetup flags alone left stale cooked triangles in the first diagnostic. the port override uses the installed public `EditorStaticMeshLibrary.remove_collisions_with_notification(mesh, true)` on the exact owned source mesh to rebuild its collision. its recorded simple/convex counts remain zero, lod 0 section 0 retains collision, and double-sided complex-as-simple flags remain set. the successful pre-save and saved-map trace brackets prove both sides of the sloping shell block while the hollow eave remains clear; they do not prove live pawn/ball behavior.

the subsequent `.local/hlck/dungeon-anchor-inspection.json` passed **27/27** checks with the amended map chain and native roof queries. the fresh play preflight again passed all 27 checks, and `.local/hlck/dungeon-registration-plan.json` verified the existing registration and saved sql without registering again. the separate runtime result below now proves native player spawn and exit setup; entry and return travel remain untested.

an earlier unsaved stage was blocked by strict actor preservation: the lower-net label edit also changed its object name, and the diagnostic found an extra scenerig camera manager and a fog-height change. its failed receipt, snapshot diagnostic and byte backups remain intact. [recover_hlck_roof_attempt.py](../Tools/recover_hlck_roof_attempt.py) checked both saved-map hashes, backup bytes, the exact diagnosed actor state, and the sole dirty arena before discarding that unsaved attempt. native recovery passed in `.local/hlck/pyramid-net-recovery/20260915-011732-311999-a0f168d1/result.json`; saved imports, mod content and installed files remained unchanged. twelve portable recovery refusal tests also pass.

separate no-edit observations of each unchanged map, immediately after load and at approximately 0.5, 2 and 5 seconds, found no actor deltas and no saved content changes. their receipts are `.local/hlck/map-reload-observation/20260915-011837-416491-3509ea10/result.json` and `20260915-011917-575302-af8bd726/result.json`. the earlier SceneRig/fog cause remains unproven. the successful retry passed the strict original actor comparisons without a class whitelist, fog normalization or unexplained actor changes.

a native roof dry run stopped before map processing when git checkout converted `Mod/Basketbroom/Content/ModEdits.sql` from its recorded 275-byte lf form to 276 bytes with CRLF. the original commit, index and two pre-play backups matched the unchanged registration receipt byte for byte; only the final carriage return differed. the current sql was backed up locally and restored from the verified 275-byte copy. an exact-path `.gitattributes` rule now preserves lf for this file. the strict registration hash check and its historical receipt remain unchanged; this repair does not re-register the dungeon or edit a database. evidence is retained under `.local/hlck/sql-eol-recovery/`.

the portable source validator passes for all 23 import entries: 18 mesh sources, four audio cues and the original basalt texture. the roof source check passes for its exact four-face shell.

thirteen new tests in [test_hlck_pyramid_net_receipts.py](../Tools/test_hlck_pyramid_net_receipts.py) pass. they cover the seven-package destination boundary, repeated legitimate roof amendments, changed maps, changed byte backups, modified receipts, failed and dry-run attempts, missing preservation proof and unrelated original/map identities. the existing 18 pie lifecycle/recovery/database-backup cases and six registration-receipt cases also pass. these are portable safeguards; they do not substitute for native imports or Play.

a read-only machine sample before this native session on september 14 found 31.81 gib physical memory with 18.40 gib free and no creator kit, unreal editor or shader-worker process at that moment. the kit's `Engine/DerivedDataCache/Boot.ddc` was 96,587,811 bytes, the common unreal deriveddatacache directory existed, and the sampled phoenix log metadata belonged to the earlier september 12 session. those are historical readiness observations, not current free-memory or shader-readiness claims. the earlier first play queued more than 300,000 shaders and briefly left less than 1 gib free. no account identifiers, tokens or raw authentication logs were read for this readiness check.

## native play stall diagnosis (september 14 evening)

the 21:56 native editor session created the correct `basketbroom_dungeonmap` pie world at 21:57. its play startup discovered large batches of missing native skinfx material shader maps. the editor alternated between unresponsive and responsive while shader workers continued executing; this is evidence of shader compilation and severe memory pressure, not a completed gameplay test or a demonstrated deadlock in the basketbroom roof.

the read-only [Inspect-HLCKStartup.ps1](../Tools/Inspect-HLCKStartup.ps1) now records one process-identity-checked snapshot of the native editor, its directly parented shader workers, machine memory and narrowly filtered startup log evidence. run `powershell -noprofile -executionpolicy bypass -file Tools/Inspect-HLCKStartup.ps1 -save` from the repo. reports remain in ignored `.local/hlck/startup/`. it recognizes both exact installed executable paths (`UE4Editor.exe` and epic's `HogwartsLegacyCreatorKit.exe`), rechecks each sampled process identity, and reports multiple editors without selecting one. it compares the installed `PhoenixGame/Saved/Logs/Phoenix.log` with `%LOCALAPPDATA%/HogwartsLegacyCreatorKit/Saved/Logs/Phoenix.log`, reporting both candidates and selecting the newest write time; equal timestamps prefer the installed candidate. recency does not prove which editor wrote the log. fifteen synthetic checks in [Test-HLCKStartup.ps1](../Tools/Test-HLCKStartup.ps1) cover selection, stale evidence, sanitization and process attribution. a queue sample can increase as more materials load; it is not an ETA. missing log entries and windows' `responding` flag do not prove that pie has ended or gameplay is healthy.

at 22:11:53 edt, the saved sample recorded 318,581 pending shaders, 20 workers, 43.24 gib editor private memory, 0.77 gib available physical memory and 63.72 / 64.93 gib system commit. the user subsequently reported returning to the editor with Stop/Esc. by 22:15 the pending queue had decreased to 313,914 and available physical memory recovered to 11.64 gib, although system commit remained high at 65.74 / 66.95 GiB. these are historical observations, not a completed recovery or shader-cache proof. the exact snapshots are `20260914-221153-547-8bebd4ec.json` and `20260914-221515-526-551b5d1a.json` in that report folder.

for that stalled attempt, recovery kept the editor idle while the queue drained and checked memory, disk headroom and actual editor-world state before another attempt. epic documents asynchronous material shader compilation and caching of completed output in [UE4.27 shader development](https://dev.epicgames.com/documentation/en-us/unreal-engine/shader-development?application_version=4.27) and the [derived data cache](https://dev.epicgames.com/documentation/en-us/unreal-engine/derived-data-cache?application_version=4.27). restarting does not prove unfinished shader jobs are reusable. this session's log opened the shipped compressed cache and a writable local cache successfully, so no cache deletion is indicated.

disk headroom also fell to 6.84 gib during compilation. a scoped cleanup recovered 8.502 gib by removing 650 byte-identical duplicate files from 15 older `.local/PackageWork/Development-*/Stage/Windows` folders. every retained archive counterpart was sha256-checked against its staging copy immediately before deletion and held against modification during that removal. all build archives, the current package/staging directory, videos, logs, manifests, cooked data and directories were preserved. the ignored cleanup receipt and verification are under `.local/duplicate-stage-cleanup/20260914-222200-315/`. at 22:27 edt the editor was responding, C: had 15.31 gib free, and 262,504 shaders remained queued; this still does not constitute a passed play test.

no shader-worker override, preload bypass, rendering-quality reduction or process termination was applied. a proposed lower worker count was withdrawn: it does not bound the much larger editor-side job queue and can slow drainage. `SkinFX.DisableForceFullEditor` concerns opening skinfx's full blueprint editor, according to its installed help text; it is not a preload control. native preload toggles remain unchanged because their gameplay consequences have not been established.

the old bridge response at 21:51 belongs to an earlier editor and failed the owned-map preflight before running runtime checks. it is not the result of the later manual play attempt. [probe_editor_checkpoint.py](../Tools/probe_editor_checkpoint.py) now includes a utc timestamp, process id, editor world and pie-world inventory to distinguish fresh live state from earlier files. a harmless fresh checkpoint request replaced the pending stop request before asking the user to reconnect the bridge. at that point, native spawn, exit readiness and final cleanup still required fresh evidence. the september 15 play result below subsequently verified those checks; it does not establish a general cache-reuse guarantee.

## live reconnect and cleanup reporting

on september 14 at 22:35 edt, the reconnected bridge verified the same native editor process with the owned dungeon restored, no pie worlds and no dirty map/content packages. separate public queries at 22:45 returned `has_active_editor_mod_bp() == false` and an empty active-mod name. the loaded `/Basketbroom/` map alone does not establish active mod selection. the user subsequently selected the existing basketbroom mod using the supported toolbar; no active-mod setter was identified in the installed reflected python surface. fresh cleanup, registration and normal-play checks then verified the active mod on september 15.

[probe_editor_checkpoint.py](../Tools/probe_editor_checkpoint.py) now reports the editor-mod flag, name and content path alongside its timestamp and process/world inventory. [probe_hlck_startup_api.py](../Tools/probe_hlck_startup_api.py) records native reflected api documentation without invoking loading, selection, staging or publishing functions. this kit exposes `automation_wait_for_loading`, but no verified shader-completion boolean query was found. a missing console variable can also read as zero, so an invented variable must not be treated as shader readiness.

a real missing-mod failure exposed a stale-report issue in [inspect_hlck_pie_cleanup.py](../Tools/inspect_hlck_pie_cleanup.py): it raised before replacing the september 12 success receipt. the helper now publishes an atomic `inspecting` receipt with `clean_owned_editor=false` before its first engine query, then a fresh passed/failed result with attempt id, process id, timestamps and error. Project/mod/world ownership, no pie, no dirty packages and no registered observer remain mandatory. brief windows reader locks have bounded retries; unrecoverable report I/O errors propagate rather than returning success. the live missing-mod failure at 22:44 correctly replaced the old success with a fresh failed receipt. this was a successful test of failure reporting, not a passed native cleanup validation.

all 13 focused [cleanup-receipt tests](../Tools/test_hlck_pie_cleanup_receipts.py) and the 18 existing pie lifecycle tests pass. the focused tests cover old-success replacement, wrong project/mod, query errors, each cleanup blocker, read-only behavior and file-lock/atomic-write handling. no new native play session, asset save, database query or registration occurred during that september 14 reconnect diagnosis. it precedes the passed september 15 runtime check below.

## normal play passed (september 15)

the corrected observer completed at **2026/09/15T08:30:15.931158Z**, with **16/16 runtime checks passed**. the retained reports are under `.local/hlck/pie-success/20260915-083015-931158/`, with `.local/hlck/native-play-success.json` as the success pointer. the fresh working receipts are `dungeon-pie-smoke.json`, `dungeon-pie-cleanup.json` and `dungeon-registration-plan.json` in `.local/hlck/`.

normal editor play began creating the owned dungeon pie world at 08:30:00.928 utc and finished world creation at 08:30:01.631. the observer identified it at 08:30:03.634856. it verified the native phoenix game mode, game instance and player controller, a possessed `bp_biped_player` grounded at `(-3000, 0, 90.15)` centimeters, settled native services, the existing entrance row, the ugc registry, runtime dungeon/subdivision extensions and the owned exit actor's interaction components and first-return defaults. no travel or player input was injected.

the observer requested end play, logged at 08:30:11.404 utc, then completed its preservation checks. the independent cleanup receipt at **08:31:27.783266 utc** passed with basketbroom active, the original dungeon open, no pie world, no dirty packages and no registered observer. mod content, protected static database files and installed maps remained unchanged. `PhoenixDynData.sqlite` changed hash during native runtime; its **logical delta has not been verified**. pre-play database bytes are preserved in `.local/hlck/pie-smoke-backups/20260915-082910-829023/`; no installed database was restored.

machine samples at 08:26 and 08:31 utc found no shader-worker processes. the last logged queue count of one belonged to 03:26 and is historical, not a current remaining-job count. this successful short play attempt supersedes the earlier pending-spawn status; it does not prove sustained performance or eliminate all future shader stalls.

the scope is native spawn, runtime integration readiness and clean shutdown. it does **not** establish actual entrance/return interaction, scoring, catching, a full match or multiplayer.

## epic-launched session after reboot (september 15)

the post-reboot session passed **17/17 current-runtime checks** at **2026/09/15T10:41:35.877765Z**. this is an observation of the already-running play session: the same 16 native checks passed, plus a separate advancing-world-clock check. during 8.281 seconds of fresh observation, game time advanced 8.394 seconds; all checks stayed satisfied for 2.625 seconds. the native player remained possessed and grounded at `(-3000, 0, 90.15)` cm. this establishes current responsiveness and integration readiness, not cold startup duration, sustained frame rate, actual travel or pre-play file preservation.

[inspect_hlck_live_pie.py](../Tools/inspect_hlck_live_pie.py) reuses only the original smoke test's read-only runtime snapshot. it requires the exact native project, active-mod flag/name, one owned pie world, no dirty packages and no other smoke observer. it observes actual elapsed time, records a fresh attempt/PID/timestamps before checking, bounds observation to 30 seconds, and unregisters itself. a blocked engine thread can still delay a callback past that deadline. the helper never starts or stops play and never replaces the older startup success. its 25 focused tests cover session refusal, real observation windows, paused/stalled time, callback cleanup, timeout and report-write failures.

after the observation, the separate owned-session helper stopped play normally. fresh cleanup passed at **10:42:31.768292 utc**, all **27 saved dungeon/anchor checks passed again**, and the 10:43:29 checkpoint confirmed the original dungeon open, basketbroom active, no pie and no dirty packages. the independent archive is `.local/hlck/live-pie-success/e0eb92cc52b04aacbebf7bc03090aa2f/`, with pointer `.local/hlck/native-live-pie-success.json`. the earlier startup archive and pointer remain unchanged. all 28 material files that were already changed when this turn began were backed up and sha256-verified unchanged during this work; they remain separate from these tool/document changes.

this session exposed two errors in the old os diagnostic. epic launched `Engine/Binaries/Win64/HogwartsLegacyCreatorKit.exe`, while it recognized only `UE4Editor.exe`; the fresh log was under `%LOCALAPPDATA%/HogwartsLegacyCreatorKit/Saved/Logs/`, while it read only the older installation log. the corrected schema-2 inspector recognizes both exact executable paths, retains parent/creation-time worker attribution, and selects the newest of both known log files with explicit ownership/age caveats. **15 synthetic regression checks pass.** its fresh 10:43:29 sample identifies pid 2452, a responding editor, zero shader workers and 12.41 gib available on C:.

the two launch sessions also logged different writable local shader-cache paths. the earlier `UE4Editor.exe` session used `../../../Engine/DerivedDataCache`; the epic-launched session used `%LOCALAPPDATA%/UnrealEngine/Common/DerivedDataCache`. this difference is a plausible explanation for the repeated compilation, not proof of its full cause or a cache-reuse guarantee. neither cache was deleted, merged, relocated or reconfigured. continue through epic's authenticated launch path. screen control still failed during node initialization after a reset; the already-connected editor bridge performed this work.

[probe_hlck_gameplay_api.py](../Tools/probe_hlck_gameplay_api.py) also inspected the existing player's 43 components and the kit's reflected broom, toolset, RPG/Able ability, input and blueprint-authoring signatures. it found native `BroomItemTool.spawn_and_mount_broom`, native ability execution and the existing player spell-group query, along with the installed gameplay/ability components. these are candidate integration interfaces only: no broom spawn, spell cast, asset load, graph change or new gameplay adapter was attempted by this probe. that probe preceded the arena expansion; its later saved-map and play-observation results are recorded below and in [arena-volume-expansion.md](arena-volume-expansion.md).

## expanded arena and existing play passed (september 15)

both native maps were enlarged to 1.45 times their original enclosed volume and saved/reloaded through the bounded expansion stage. it changed ten mesh packages and two maps, preserving existing actor identities, material-file bytes, sporting dimensions, PlayerStart/exit locations and native registration. **356 real collision queries passed**, followed by **27/27 dungeon/anchor checks**. the successful amendment is `.local/hlck/arena-expansion-stage/20260915-113602-895613-1a969573/result.json`; its verified map chains retain the original roof/anchor evidence. [arena volume expansion](arena-volume-expansion.md#native-saved-map-acceptance) records the exact scope, qualified native reload behavior and preservation limits.

in process **20652**, the read-only live observer sampled the already-running, user-started enlarged dungeon from **2026/09/15T12:07:40.332610Z** to **12:07:47.931909Z**. it passed **17/17 checks**: the original 16 native player/runtime checks and a separately measured advancing world clock. game time advanced **7.698 seconds during 7.593 seconds** of fresh observation, and all checks remained satisfied for two seconds. the observer did not start or stop play, inject controls, save assets or register anything.

after that observation, the separate owned-session stop helper ended the exact dungeon pie session normally. cleanup passed at **12:09:05.354259 utc** with no pie, dirty packages or observer. the expanded saved dungeon/anchor inspection passed **27/27** at **12:09:06 utc**; the **12:09:06.714716 utc** checkpoint confirmed the original dungeon open and basketbroom active in the same process.

the independent archive is `.local/hlck/expanded-live-pie-success/413b9c661df6493bbb98da19a246848d/`. its `manifest.json` verifies all six archived observation, stop, cleanup, anchor and checkpoint receipts; manifest sha-256 is `794c47e5da55f7c06366330cd6fb1a6453460ed35f5526722021612ec0d96e48`. `.local/hlck/native-expanded-live-pie-success.json` points to this archive. the earlier startup and pre-expansion live-observation archives and pointers remain unchanged.

this is evidence of **current native responsiveness and integration readiness in the enlarged arena**, followed by clean shutdown. the session was already running when observation began, so it supplies **no startup or pre-play preservation proof**. it also does not establish sustained frame rate, entry/return travel, scoring, catching, a full match or hogwarts multiplayer.

## remaining integration work

the next integration proofs are actual entry and return through native interactions and a small mod-owned **blueprint score/catch adapter**, authored and compiled in the licensee 4.27 creator kit against existing exposed native classes and events. scope it to the arena through the supported gameplay-mod/mutator path. the exact native broom, input and catch-event interfaces still need inspection and a runtime proof; event-graph wiring has not been completed. here, compilation means blueprint asset compilation, not adding a c++ gameplay class or shipping a native DLL.

the installed kit's public `PhoenixGame/Config/DefaultEditor.ini`, under `[phoenixugc]`, explicitly lists `dll`, `exe`, `obj` and `so` as disallowed file types (lines 16, 17, 26 and 35). its shipped gameplay examples are `/PhoenixUGC/GameplayMod/Blueprints/MM_ExampleGameplayMod` and `AC_ExampleGameplayMod`. the installed editor authoring example `PhoenixGame/Tools/Scripts/EditorPython/PEEVES/Scripts/Tools/BlueprintComponentEditor/subblueprint_editor.py` uses `unreal.PhxEditorBlueprintLibrary.add_component_of_type` at line 85 and `compile_blueprint` at line 94. these are primary local evidence for blueprint authoring with the kit's existing native APIs. they do not establish an officially supported route for new mod-supplied c++ modules; publisher-provided runtime plugins are not evidence that a standalone game dll can be loaded by a content mod. these paths are relative to `C:/Program Files/HogwartsLegacyCreatorKit/`.

the requested volume increase is implemented at **45%**, with native saved-map and existing-session play checks passed as recorded above. full regulation, remaining spell adapters, native entry/return, navigation/minimap, cloud-cooked testing and final art still need their separate runtime proofs. the standalone c++ implementation and its multiplayer transport cannot be assumed to run inside the licensee creator Kit.
